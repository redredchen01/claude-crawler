# Claude Crawler 代码优化计划

## 目标
根据代码审查结果，优化项目配置和代码质量

## 优化项目

### 1. 添加缺失依赖 (pyproject.toml)
- 添加 `pandas>=2.0.0` (app.py使用但未声明)

### 2. 修正安全配置默认值 (crawler/config.py:58)
- 将 `ALLOW_PRIVATE_HOSTS` 从 `True` 改为支持环境变量
- 生产环境默认禁止访问私有地址

### 3. 修复计数器竞态 (crawler/core/engine.py:372-376)
- 将cache统计操作移入锁内，确保线程安全

### 4. 添加数据库索引 (crawler/storage.py)
- `pages(scan_job_id, status)` - 加速pending页面查询
- `resources(scan_job_id, popularity_score)` - 加速排序查询
- `http_cache(cached_at)` - 加速缓存清理

### 5. 添加缓存过期清理 (crawler/cache.py)
- 新增 `cleanup_expired(max_age_days=7)` 方法
- 防止http_cache表无限增长

### 6. 修正README测试数量 (README.md:12)
- 声称606个测试，实际应验证真实数量