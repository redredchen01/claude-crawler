# /skx — Skill Ideation Report
Date: 2026-04-06 (Session 5)

## Vault Signals
- Notes: 97 | Ideas: 6 | Active TODOs: 253 | Orphans: 3 (目標: 0)
- Top tags without skills: `reference`(37), `workflow`(2), `maintenance`(1)
- Recent focus: sub2api-deploy, remotion-clip, weekly-report-workbench(新 area), site-ops
- Active areas: ai-automation, automation-rhythm, skill-development, weekly-report-workbench

## Git Patterns
- GWX: feat(auth) export/import (VPS token transfer) — no skill wraps it
- YDK: v0.5.0 llm module added — `/ydk-module` exists for scaffolding but no runner skill
- TG Bot: modularize + wrapCmd utility — stable, no new skill needed
- MAIN: vault-mining TF-IDF engine complete ✅

---

## P1 — High Signal (build this week)

| # | Skill Name | Trigger | Description | Tools |
|---|-----------|---------|-------------|-------|
| 1 | `/sub2api-ops` | "起 sub2api", "sub2api 環境" | Docker Compose 管理 Sub2API（start/stop/logs/health） | Bash |
| 2 | `/weekly-prep` | "整理本週內容", "周報原料" | 從 journal/git/incident 聚合周報素材，輸出草稿清單 | Bash, Read, Glob |
| 3 | `/ctx-manage` | "ctx 操作", "壓縮 context" | CTX CLI 快速包裝（context window compress/status/reset） | Bash |

**Why P1**:
- `/sub2api-ops`: `sub2api-deploy` 是今天最新 active project（2026-04-06 更新），Docker 環境起停是高頻操作，零 skill 覆蓋
- `/weekly-prep`: `weekly-report-workbench` area **今天新建**，明確工作流需求（daily → 升格 → 周報收口），3 個 related 筆記支持
- `/ctx-manage`: CTX v0.1.0 active CLI project，GitHub 公開，有 `/context-compact` 和 `/context-boost` 但無 ctx CLI 直接操作 skill

## P2 — Medium Signal (build next)

| # | Skill Name | Trigger | Description | Tools |
|---|-----------|---------|-------------|-------|
| 1 | `/static-ghost-run` | "去水印", "batch 去水印" | Static Ghost v0.4.2 批量水印移除（Python CLI + 進度追蹤） | Bash |
| 2 | `/gwx-auth` | "gwx 認證", "轉移 token" | GWX auth export/import（VPS token 轉移，v0.25.0 新功能） | Bash |
| 3 | `/ydk` | "ydk help", "ydk 常用" | YDK CLI runner 快速查閱（v0.5.0 subcommands + llm module） | Bash |

**Why P2**:
- `/static-ghost-run`: project active + v0.4.2，但 skill 不急
- `/gwx-auth`: 最近 gwx commit 新增，VPS 部署場景需要，但不頻繁
- `/ydk`: v0.5.0 llm module 新，`/ydk-module` 只做 scaffold，缺少 runner

## P3 — Backlog
- `/sub2api-check` — 輕量健康檢查（HTTP probe only）
- `/reference-audit` — 37 個 reference 筆記品質審計（無 link 的 reference 清理）
- `/automation-rhythm-check` — 驗證當天自動化節奏是否執行

---

## Quick Wins (< 20 min)
- `/ydk` — 包裝 ydk CLI，15 行
- `/gwx-auth` — gwx auth export/import，20 行
- `/sub2api-check` — curl health probe，10 行

## Tool Chain Gaps
- `sub2api-deploy` ↔ `test-ydapi`: 有測試環境但無自動化啟動/健康確認 skill
- `weekly-report-workbench` ↔ `weekly-digest`: 缺收口步驟（prep → digest 中間無 skill）
- `static-ghost` ↔ `vault`: 去水印操作結果無 log 記錄機制
